import React, { Fragment } from "react";
import { Form } from "../../../components/Form";

const Register = () => {
    return (
        <Fragment>
            <Form />
        </Fragment>
    );
};
export default Register;
